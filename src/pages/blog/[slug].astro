---
import { getCollection } from 'astro:content';

// 1. Generamos las rutas para cada post
export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post: any, index: number) => ({
    params: { slug: post.slug },
    props: { 
      post,
      // Pasamos el post anterior y el siguiente para las flechas
      prevPost: index > 0 ? posts[index - 1] : null,
      nextPost: index < posts.length - 1 ? posts[index + 1] : null
    },
  }));
}

// 2. Recogemos los datos del post
const { post, prevPost, nextPost } = Astro.props as any;
const { Content } = await post.render();
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>{post.data.title}</title>
  </head>
  <body>

    <header class="barra-superior">
      <div class="header-contenido">
        <div class="usuario">
          <div class="avatar">LB</div>
          <div class="datos">
            <span class="nombre">Lluc Belvis Balsera</span>
            <span class="correo">llucbelvis@gmail.com</span>
          </div>
        </div>
        <nav class="menu-vacio">
          <span>Official Blog</span>
        </nav>
      </div>
    </header>

    <main class="contenedor-principal">
        <div class="controles-superiores">
          <a href="/" class="volver">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
            Home
          </a>
          
          <nav class="navegacion-superior">
            {prevPost ? (
              <a href={`/blog/${prevPost.slug}`} class="nav-link">← Previous</a>
            ) : <span class="disabled">← Previous</span>} 
            
            <span class="separador">|</span>
            
            {nextPost ? (
              <a href={`/blog/${nextPost.slug}`} class="nav-link">Next →</a>
            ) : <span class="disabled">Next →</span>}
          </nav>
        </div>

        <article>
          <h1>{post.data.title}</h1>
          <p class="fecha">{post.data.date.toLocaleDateString()}</p>
          <hr />
          <Content /> 
        </article>
      </main>
  </body>
</html>

<style is:global>
  body {
    font-family: 'Inter', system-ui, -apple-system, sans-serif; /* Más moderna que Georgia */
    line-height: 1.7;
    color: #2d3436;
    background-color: #ffffff;
    margin: 0;
    padding: 0;
  }

  h1 {
    font-size: 2.8rem;
    font-weight: 800;
    color: #1a1a1a;
    line-height: 1.2;
    margin-bottom: 10px;
  }

  .fecha {
    text-transform: uppercase;
    font-weight: bold;
    letter-spacing: 1px;
    color: #636e72;
    font-size: 0.9rem;
  }

  /* Estilo para las imágenes */
  article img {
    width: 100%;
    border-radius: 8px;
    margin: 30px 0;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  }


  .btn {
    text-decoration: none;
    color: #1a1a1a;
    font-weight: 700;
    font-size: 1.1rem;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s;
  }

  .btn:hover {
    transform: translateY(-3px);
    color: #0984e3;
  }

  .btn span {
    font-size: 0.8rem;
    color: #b2bec3;
    text-transform: uppercase;
  }
  .barra-superior {
    width: 100%;
    border-bottom: 1px solid #f1f2f6;
    padding: 15px 0;
    background: #fff;
  }

  .header-contenido {
    max-width: 850px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 20px;
  }

  .usuario {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .avatar {
    width: 40px;
    height: 40px;
    background-color: #dfe6e9; /* Gris suave */
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-weight: bold;
    color: #636e72;
    font-size: 0.8rem;
  }

  .datos {
    display: flex;
    flex-direction: column;
  }

  .nombre {
    font-weight: 800;
    font-size: 0.9rem;
    color: #1a1a1a;
    line-height: 1.2;
  }

  .correo {
    font-size: 0.75rem;
    color: #b2bec3;
  }

  .menu-vacio {
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #0984e3; /* Azul Polymorph */
  }
 /* Contenedor centralizado para que todo esté alineado */
  .contenedor-principal {
    max-width: 900px;
    margin: 30px auto; /* Reducido de 60px a 30px */
    padding: 0 20px;
  }

  /* Fila que junta el botón volver y las flechas */
  .controles-superiores {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px; /* Espacio pequeño antes del título */
    padding-bottom: 10px;
    border-bottom: 1px solid #f1f2f6;
  }

  /* Estilo minimalista para las flechas de arriba */
  .navegacion-superior {
    display: flex;
    gap: 15px;
    align-items: center;
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
  }

  .nav-link {
    text-decoration: none;
    color: #0984e3;
    transition: transform 0.2s;
  }

  .nav-link:hover { transform: translateY(-2px); }

  .separador { color: #dfe6e9; }
  
  .disabled { color: #b2bec3; cursor: not-allowed; }

  /* Ajuste del artículo para que no tenga margen superior extra */
  article {
    margin-top: 20px;
  }

  /* El botón volver ahora es más compacto */
  .volver {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    text-decoration: none;
    color: #636e72;
    font-weight: 700;
    font-size: 0.8rem;
    text-transform: uppercase;
    transition: all 0.2s ease;
  }
  
  .volver:hover { color: #0984e3; transform: translateX(-3px); }
</style>